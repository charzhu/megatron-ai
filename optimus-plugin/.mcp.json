{
  "mcpServers": {
    "optimus-facade": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/mcp-server.js"],
      "env": {
        "DOTENV_PATH": "${CLAUDE_PROJECT_ROOT}/.env"
      }
    }
  }
}